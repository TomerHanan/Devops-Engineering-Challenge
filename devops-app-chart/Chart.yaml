apiVersion: v2
name: devops-app-chart
description: A Helm chart for Spring Boot and MySQL
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: mysql
    version: "12.x.x" # Use the latest 12.x version
    repository: "https://charts.bitnami.com/bitnami"