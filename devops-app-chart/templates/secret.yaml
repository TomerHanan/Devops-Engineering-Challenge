apiVersion: v1
kind: Secret
metadata:
  name: {{ include "devops-app-chart.fullname" . }}-db-secret
type: Opaque
data:
  # These must match the values in values.yaml
  db-username: {{ "devops_user" | b64enc | quote }}
  db-password: {{ "devops_password" | b64enc | quote }}